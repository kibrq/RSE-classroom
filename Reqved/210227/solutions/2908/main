#!/usr/bin/env python3

'''
Напишите программу, которая ищет среди целых чисел, принадлежащих числовому отрезку [586132; 586430], числа, имеющие максимальное количество различных делителей. Найдите минимальное и максимальное из таких чисел. Для каждого из них в отдельной строчке выведите количество делителей и наибольший делитель, не равный самому числу.
'''


def divisors(n):
    divs = []
    for x in range(1, int(n ** 0.5) + 1):
        if n % x == 0:
            divs.append(x)
            if x != n // x:
                divs.append(n // x)
    return divs


'''
В ans[0] хранится минимальное число с текущим наибольшим кол-во делителей
В ans[1] хранится максимальное число с =//=
В ans[2] хранится текущее наибольшее число делителей
'''

ans = [None, None, 0]


def test(n):
    global ans
    divs = divisors(n)
    if ans[2] < len(divs):
        ans = [divs, divs, len(divs)]
    elif ans[2] == len(divs):
        ans[1] = divs


for n in range(586132, 586430 + 1):
    test(n)

print(ans[2], sorted(ans[0])[-2])
print(ans[2], sorted(ans[1])[-2])
