0. 
  Напиши функцию, которая печатает на экран все цифры числа, в любом порядке.
1. 
  Напиши функцию max_of_list, которая принимает переменную arr, и ищущую внутри этого arr максимальное число
  Указание:
    Тоже самое, что было на занятии, мы искали минимум, сумму, теперь максимум. Но внимательно: мы искали эти штуки в массиве цифр, а тут тебе уже дают массив.

2.
  Вот функция gcd(a, b)

  def gcd(a, b):
    a, b = max(a, b), min(a, b)
    if b == 0:
      return a
    return gcd(b, a % b)

  Данная функция ищет наибольший общий делитель чисел a и b. Она обладает свойством таким же, как и сумма и минимум и максимум. То есть:
    gcd(a, b, c, d) = gcd(a, gcd(b, gcd(c, d)))

  Тебе надо написать функцию gcd_of_list, которая принимает arr, и ищет наибольший общий делитель всех чисел.

Общее: обязательно попробуй запустить на каких-нибудь примерах и проверь работает ли.
