Определите, сколько символов * выведет эта процедура при вызове F(140):
def F( n ):
  print('*')
  if n >= 1:
    print('*')
    F(n-1)
    F(n//2)

Ответ: 3279892



Определите наименьшее значение n, при котором сумма чисел, которые будут выведены при вызове F(n), будет больше 1000000.
 Запишите в ответе сначала найденное значение n, а затем через пробел – соответствующую сумму выведенных чисел.

def F( n ):
  print(n+1)
  if n > 1:
    print(n+5)
    F(n-1)
    F(n-2)

Ответ: 24 1114369


Ниже записаны две рекурсивные функции (процедуры): F и G.
Сколько символов «звёздочка» будет напечатано на экране при выполнении вызова F(12)?

def F(n):
  print("*")
  if n > 0: 
    print("*")    
    G(n - 1)
def G(n):
  print("*")
  if n > 1: 
    print("*")    
    F(n - 2)

Ответ: 17



НЕ ПО ТЕМЕ

Определите наименьшее значение суммы n+m такое, что значение F(n, m) больше числа 15 и выполняется условие n и m – натуральные числа. 
Запишите в ответе значение этой суммы.

def F(n, m):
 if n < m:
  n, m = m, n               # Означает, что теперь в n лежит значение m, а в m лежит значение n
 if n != m: 
   return F(n-m,m)
 else: 
   return n

Ответ: 32
